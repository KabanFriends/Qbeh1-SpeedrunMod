using HarmonyLib;

namespace Qbeh1_SpeedrunMod.Patches
{
    public class HUDFpsPatch
    {
        /*
        [HarmonyPatch(typeof(HUDFps), "Start")]
        [HarmonyPrefix]
        private static bool StartPatch()
        {
            return false;
        }

        [HarmonyPatch(typeof(HUDFps), "Update")]
        [HarmonyPrefix]
        private static bool UpdatePatch()
        {
            return false;
        }

        [HarmonyPatch(typeof(HUDFps), "Start")]
        [HarmonyPrefix]
        private static bool OnGUI()
        {
            return false;
        }
        */
    }
}
